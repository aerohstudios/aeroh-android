<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AerohAndroid" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/secondary_black</item>
    </style>
    <style name="Theme.SplashActivity" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/primary_yellow</item>
    </style>
</resources>
